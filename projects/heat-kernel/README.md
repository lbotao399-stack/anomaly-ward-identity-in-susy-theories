# Project 3 — Non-perturbative heat-kernel / full-BV

The anomaly from a heat-kernel / full-BV treatment rather than a diagram-by-diagram
expansion — the regulator-independent, non-perturbative angle. Targets the same coefficient
without a fixed Feynman-diagram slice, and is the natural setting for the covariant-completion
statement (the threshold sector is a Seeley–DeWitt coefficient).

**Status: seed.** The bounded heat-kernel lemma was admitted in the #71 audit; the
`heat-kernel-n4-one-loop-memo` sketches the route. Most of the project is future work.

- `readable/` (to add) — the heat-kernel derivation of the one-loop anomaly and its relation
  to the DRED cutting-failure master $1/32\pi^2$.
- `machine/` — heat-kernel verifiers (Phase 2).

Legacy sources: `proposals/heat-kernel-n4-one-loop-memo`, the Full-BV heat-kernel line, the
covariant-completion memo's (S2) trace-covariance statement.
