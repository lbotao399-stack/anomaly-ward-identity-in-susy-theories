# machine/ index — heat-kernel

Machine-verification files for the **heat-kernel** project (root tree; option C — see
`../../README.md`). Do not read these for the physics — read `../readable/`.

## scripts/
```
scripts/verify_heat_kernel_n4_one_loop_memo.py
```
## audits/
```
audits/heat-kernel-ordered-simplex-verification.json
```

Most of this project is future work; the bounded heat-kernel lemma admitted in the #71 audit
and the `proposals/heat-kernel-n4-one-loop-memo` are its current seeds.
