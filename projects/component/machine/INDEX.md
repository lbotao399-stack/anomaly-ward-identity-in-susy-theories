# machine/ index — component

The **component** project has no dedicated machine files on `main` yet. It shares the
component-reconstruction foundation with `notation`:

```
scripts/verify_step3b_component_reconstruction.py
audits/step3b-component-reconstruction-verification.json  (+ gap-audit, notation-ledger, independent-review)
contracts/foundations/step-03b-component-reconstruction.md   (readable source)
```

The component one-loop bi-letter calculation and its verifiers are the main open task; they
will be indexed here as they are added.
