# Step 5 WW contact quotient

$$
N_{I_3}=30,\qquad N_{\widetilde S_3}=6,\qquad N_{\mathrm{pair}}=30\cdot6\cdot2=360.
$$

$$
N_{I_4}=180,\qquad k+(-k)=0.
$$

$$
r_0=k,\qquad r_1=q-k,\qquad (-p)+(-k)+(k-q)+(p+q)=0.
$$

Decorated classes: 360 bubble, 180 tadpole; physical canonicalization is open.

The quotient is structural only:

$$
\mathcal G_{360}\twoheadrightarrow R_{I_3\widetilde S_3},\qquad \mathcal T_{180}\twoheadrightarrow R_{I_4};\qquad C_{\rm fiber}\ \text{not evaluated}.
$$

## Open proof obligations

- `OPEN_CONTACT_001`: Compile every joined derivative word through edge-tagged D-algebra and bind the external X/TildeW projector normalization and extraction sign.
- `OPEN_CONTACT_002`: Compile each retained source color AST and kappa inverse edge contraction to an explicit free-index color tensor.
- `OPEN_CONTACT_003`: Bind the Euclidean path-integral action-vertex expansion factor to the S3 source coefficient without importing the legacy aggregate sign.
- `OPEN_CONTACT_004`: Resolve the two I4 background ports into the ordered WW external target pair after D-algebra.
- `OPEN_CONTACT_005`: Fix the composite normal-ordering policy for the 180 I4 self-contractions.
- `OPEN_CONTACT_006`: Prove the linear amplitude identity over each structural quotient fiber; no coefficient transport is currently asserted.
- `OPEN_CONTACT_007`: Bind basis graph classes to DIRECT/REFLECTED and the eight edge-tagged trace channels.
- `OPEN_CONTACT_008`: Compute the typed automorphism group after color and derivative-word canonicalization; only then fix any separate symmetry factor.
