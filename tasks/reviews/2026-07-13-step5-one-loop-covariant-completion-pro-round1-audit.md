# Step 5 one-loop covariant-completion review — round 1 audit

## Admitted structural statements

$$
\operatorname{Loc}_{\mathrm{UV}}\Gamma_{\mathscr I}^{(1)}
\ne
\Gamma_{\mathscr I}^{(1)}.
$$

Only the UV-local cohomology class can be fixed by a primitive coefficient.
The nonlocal transverse amplitudes are independent data.

$$
\ell_2:
\mathfrak H_{\mathscr I}\longrightarrow\mathcal T_2,
\qquad
\ker\ell_2=0
$$

is required.  A class-dependent lowest nonzero jet is not an injectivity
test.

$$
\Gamma_{\mathscr I}^{(1)}
=
\frac12\operatorname{STr}(H^{-1}I),
$$

so the primitive two-background level contains

$$
I_0H_1H_1,\qquad I_0H_2,\qquad I_1H_1,\qquad I_2,\qquad \mathrm{CT}_2.
$$

All DRED evanescent source operators remain in the temporary mixing complex
until pole subtraction is complete.

## Project translations

The response used a canonical chiral-frame coefficient

$$
W_a=-\frac14\bar D^2(e^{-V}D_ae^V).
$$

It is not imported.  The Project coefficient remains

$$
\mathcal W_a=-\frac18\bar D^2\Gamma_a.
$$

For a field bridge (Q_{\mathsf V}=A Q_{\mathsf C}), the Project field-space
maps are

$$
K_{\mathsf C}=A^{\mathrm T}K_{\mathsf V}A,
\qquad
G_{\mathsf C}=A^{-1}G_{\mathsf V}A^{-\mathrm T}.
$$

Only the composed endomorphism inside the regulated supertrace is transported
by similarity.  A bare Hessian similarity is not admitted without the
field/dual-field pairing.

The independent-connection bounds refer to two different words:

$$
\deg_{\mathcal A}\mathscr I\le7,
\qquad
\deg_{\mathcal A}\mathscr O_{\mathrm{cov}}\le6.
$$

The first is the classical seed; the second is the candidate anomaly.

The finite-BV density defect remains a Step-5C obligation.  It does not block
the Step-5A reference-flat perturbative Gaussian.

## Rejected or unresolved statements

$$
\operatorname{Spin}(4)_{\mathscr I}
=
\left(\frac12,0\right)
$$

was not derived and is not admitted.  The fixed component has left weight

$$
m_L(X)=1,
\qquad
m_L\!\left(\nabla_-(XX)\right)=\frac32,
$$

and its exact irreducible projection remains a typed gate.

The response did not compute

$$
\dim\mathfrak H_{\mathscr I},
\qquad
\ker\ell_2,
\qquad
\Gamma_{\mathscr I,2}^{(1)}
$$

in Project normalization.  No coefficient and no one-loop exactness claim is
imported.
