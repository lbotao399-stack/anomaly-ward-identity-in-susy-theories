# Step 5 full-N=4 one-loop — Pro review round 6 prompt

Act only as an advisory gap reviewer.  Do not import a holomorphic-twist
target, an external physics result, or an anomaly coefficient.

Audit the following internal Project claims.

## 1. Quadratic rooted Gaussian

Starting only from

$$
G_B=(K_0+H_B)^{-1},
\qquad
\Gamma_{\mathscr I}^{(1)}[B]
=\frac12\operatorname{STr}_{\rm DRED}(G_BI_B),
$$

check that the quadratic background coefficient contains exactly six ordered
words, one global factor $1/2$, and signs

$$
+,+,-,-,-,+.
$$

## 2. Topology dictionary

Check the Project topology translation

$$
I_2\longleftrightarrow I_0,
\qquad
I_3\longleftrightarrow I_1,
\qquad
I_4\longleftrightarrow I_2,
$$

$$
S_3\longleftrightarrow H_1,
\qquad
S_4\longleftrightarrow H_2.
$$

## 3. Contact closure

Determine whether coefficient acceptance requires the exact cut identity

$$
R_{\rm cut}a_{\rm metric}+b_{\rm native}=0,
$$

rather than the isolated triangle metric pole.

## 4. Background covariance

Determine whether background covariance can relate the four intrinsic letter
families $X,Y,Z,T$, or whether it only acts covariantly inside each family.

## 5. DRED order of operations

Audit the order

$$
\text{graph sum}
\longrightarrow\text{contact closure}
\longrightarrow d\text{-dimensional tensor reduction}
\longrightarrow\operatorname{Loc}_{\rm UV}^{\rm DRED}
\longrightarrow\text{evanescent mixing}
\longrightarrow q_{4d}.
$$

## 6. Matter-sector census

Audit, but do not replace, the Project grammar census.  In particular,
separate the source-Hessian port type from the carrier fields that can run in
the loop.

## 7. Coefficient boundary

Classify the old $g^2/(64\pi^2)$ value.  Do not accept any exact coefficient
unless the complete contact, matter, and quotient gates are closed.
