# GPT Pro Gate-BB12-2: locate the exact missing one-third

Work target-blind.  Do not import the holomorphic-twist coefficient into the Feynman arithmetic.  The previous response is rejected because

$$
-\sqrt2\frac23+\sqrt2\frac13=-\frac{\sqrt2}{3},
$$

not $-\sqrt2$, and because the fixed $H_-$ flavor port already exhausts the cubic $3!$.

## Locked DRED convention

$$
d=4-2\epsilon,
\qquad
g_4=\bar g+\widetilde g,
\qquad
\mu_\ell^2:=\bar\ell^2-\ell_d^2=-\widetilde\ell^2.
$$

For every selected inverse edge $e$,

$$
\frac{r_{e,d}^2R_e}{D_0D_1D_2}
-\frac{R_e}{\prod_{j\ne e}D_j}=0,
$$

$$
\frac{\bar r_e^2R_e}{D_0D_1D_2}
-\frac{R_e}{\prod_{j\ne e}D_j}
=\frac{\mu_\ell^2R_e}{D_0D_1D_2},
$$

$$
\int_\ell^{\rm DRED}\frac{\mu_\ell^2}{D_0D_1D_2}
=\frac1{32\pi^2}.
$$

No graph-specific extra $(4-d)$ is allowed.

## Locked ordered source and complete resolvent

$$
I=(\nabla_-B_1)B_2-B_1(\nabla_-B_2),
$$

$$
B_{r,c}=B_{r,0}+gB_{r,1}+g^2B_{r,2},
$$

$$
B_{r,0}=D_+\phi_r,
\qquad
B_{r,1}=\sqrt2[D_+u,\phi_r],
$$

$$
B_{r,2}=[(D_+u)u-u(D_+u),\phi_r].
$$

The exact occurrence counts are

$$
N(I_0,I_1,I_2)=(2,6,12),
$$

and the complete order-$g^2$ one-loop support is

$$
\Gamma_{g^2}^{(1)}
=\langle I_2\rangle_0
-\hbar^{-1}\langle I_1S_3\rangle_{0,c}
-\hbar^{-1}\langle I_0S_4\rangle_{0,c}
+(2\hbar^2)^{-1}\langle I_0S_3S_3\rangle_{0,c}.
$$

Do not omit bubbles, contacts, collapsed inverse kernels, nonlinear letters, seagulls, auxiliary fields, or EOM Jacobians merely because they are not three-denominator triangles.  Prove each zero occurrence by its full regulated word.

## Independently replayed cubic parents

For the ordered source $B_1>B_2$ the three port-compatible directed parents are

$$
\begin{array}{c|c|c|c}
\text{id}&\text{vertices}&\text{topology}&\text{ordered outputs}\\ \hline
001&M_1,M_2&TMM&\phi_1,\phi_2\\
002&M_1,H_-&TMH&u,C_3\\
003&H_-,M_2&TMH&C_3,u
\end{array}
$$

Finite Grassmann differentiation gives

$$
R_{001,L}=R_{001,R}=0,
$$

$$
R_{002,L}=0,
\qquad
R_{002,R}
=(-128r_{0,+\dot2},+128r_{0,+\dot1}),
$$

with full parent

$$
N_{002,R}
=(-1024r_{0,+\dot2},+1024r_{0,+\dot1})\det r_2.
$$

The pure-antichiral endpoint is

$$
D^2\theta^2=-4,
$$

and the transported bridge representation equals the same polynomial.  It has so far been counted as one occurrence, not two.  The DRED remainder used was

$$
N_{002,R}^{\rm ev}
=(-4096r_{0,+\dot2},+4096r_{0,+\dot1})\mu_\ell^2.
$$

Route 003 is the independently rerouted ordered reflection.  The simplex shift is

$$
r_0=L+yp+z(p+q),
$$

$$
2\int_{\Sigma_2}r_0=\frac23p+\frac13q.
$$

With the currently locked canonical factors

$$
g^2
\left(+\frac{\sqrt2g}{\hbar}\right)
\left(-\frac{\sqrt2g}{\hbar}\right)
\left(\frac\hbar{16}\right)^3
\left(\frac1{2!}\right)(1+1)
=-\frac{\hbar g^4}{2048},
$$

$$
D^2\bar D_{\dot a}u\big|=-\frac{4\sqrt2}{g}D_{\dot a},
\qquad
C_3=g\widetilde\phi_3,
$$

the local replay obtains

$$
\Gamma^{(1)}_{B_1>B_2,\triangle}
=\lambda_1\mathbb F^{AB}{}_{DE}
\left[
-\frac{2i\sqrt2}{3}\langle D^D,C_3^E\rangle
+\frac{2i\sqrt2}{3}\langle C_3^D,D^E\rangle
\right].
$$

The conditional target, to be read only after the Project calculation, is

$$
\lambda_1\mathbb F^{AB}{}_{DE}
\left[
-i\sqrt2\langle D^D,C_3^E\rangle
+i\sqrt2\langle C_3^D,D^E\rangle
\right].
$$

Thus the exact unresolved vector is

$$
\delta c
=\left(-\frac{i\sqrt2}{3},+\frac{i\sqrt2}{3}\right).
$$

## Required adjudication

Produce a row-complete, reproducible calculation that decides which of the following statements first fails:

1. the transported $D^2$ endpoint is only a representation of the same selected inverse square, rather than a second marked occurrence;
2. only the $q/3$ part of $2\int r_0$ contributes to $\langle D,C_3\rangle$, while the $2p/3$ part is an EOM carrier;
3. the six $I_1S_{H_-}$ terms cancel before integration because $T_1=T_3=0$ and $T_2=T_4$;
4. the $I_2$, $I_0S_4$, gauge-fixing, ghost, auxiliary, and coincident-Jacobian sectors have no ordered $D,C_3$ port;
5. the canonical pre-$D$ and external component maps above are complete.

For item 3, equality is admissible only if the compared terms have the same color word, Koszul sign, two denominators, selected full-$d$ Schwinger contact, and external momentum routing.  A four-dimensional numerator equality before edge tagging is insufficient.

For every nonzero row print

$$
(\text{id},\text{source occurrence},\text{field loop},\text{ports},
\text{vertex/Wick/Koszul factor},\text{raw D-word},r_e,
\text{full-}d\text{ contact},\mu_\ell^2\text{ remainder},
\text{simplex moment},\text{normalized output}).
$$

End with the target-blind Project coefficient vector and only then compare with the displayed target.  Do not repair the mismatch with a fitted factor, field rescaling, supersymmetry, or holomorphic-twist covariance.
