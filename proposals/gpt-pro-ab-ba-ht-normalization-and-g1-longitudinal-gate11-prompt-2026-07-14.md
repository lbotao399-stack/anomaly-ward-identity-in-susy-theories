# GPT Pro Gate 11 — adjudicate the $G_1$ longitudinal claim and the HT normalization fork

We have now independently closed two facts by executable exact audits.  Do not
change either fact to fit a target.

## Locked fact A: $G_3$ absolute normalization

The complete color contraction, including inverse propagator metrics, is

$$
\kappa^{AU}\kappa^{BP}\kappa^{XY}(T_U)_{DX}c_{PYE}
=+i\mathbb F^{AB}{}_{DE}.
$$

There is no color $1/2$.  The raw transverse trace is

$$
T_\perp=2cW,qquad c=4096
$$

after the original measures.  Dividing by $2W$ and then multiplying another
$1/2$ repeats the same transverse-rank reduction.  The audit has 217/217 exact
checks.

The full resolvent census separately proves

$$
I_{[2]}=0,qquad I_{[1]}S_3=0,qquad I_{[0]}S_4=0
$$

by exhaustive port matching.  The only 1PI topology is $I_0S_3S_3$, with the
three edgewise parent/cut pairs

$$
(-4096,+4096,-4096)\mu_\ell^2W_e.
$$

All current/link presentations cancel pairwise and contain no inverse square.
Therefore

$$
G_{32}=-2i\sqrt2\lambda_1,qquad
G_{33}=+2i\sqrt2\lambda_1
$$

is immutable at the raw local one-loop level.  The census has 24/24 exact
artifact checks.

## First question: prove or retract the new $G_1$ family

Your Gate-10 response proposed

$$
P_L=-P_D\bar r_1^{,2}(r_0-r_2)^{\dot a},qquad
K_L=+D_0D_2(r_0-r_2)^{\dot a},
$$

which changes the selected sum from $(2,2)$ to $(1,1)$.

But the existing exact 192-row replay already evaluates

$$
L_A=-\frac12D_+\bar D^2D^2
$$

inside every VVV word.  Rowwise it verifies

$$
F_A=\alpha_A\bar r_2^{,2}+L_A,qquad
C_{A,d}=-\alpha_A r_{2,d}^2-L_A,
$$

and hence leaves only $\alpha_A\mu_\ell^2$.  Its separately drawn
$I_1S_{m3},I_1S_{g3},I_0S_{m4}$ source-resolvent presentations vanish under
exact port matching and are not independent graphs.  The selected generic
sum remains

$$
A+B=(2,2)=2(p+q).
$$

Provide a row-level executable identity that distinguishes your $N_L$ from
the already included $L_A$ and its induced contact.  You must give:

1. the exact source Hessian and action Hessian;
2. all external and internal ports and the unique Wick pairing;
3. the resolvent term ($I_{[k]}S_{n_1}\cdots S_{n_j}$) and its factorial;
4. the raw $D$-word before any trace or specialization;
5. the exact denominator assignment and selected edge;
6. the parent numerator and the functional Schwinger contact from the same
   occurrence;
7. an equality showing that the sum of the actual raw rows is exactly
   $-\bar r_1^{,2}(p+q)$ for two independent generic $(p,q)$ frames.

If $N_L$ is merely a re-presentation of the $L_A$ already contained in
$F_A$, explicitly retract the $(2,2)\to(1,1)$ correction.  A symbolic label
$R_4\to R_2$ without the seven items is not evidence.

Keep these layers distinct:

$$
\text{raw local jet},\quad
\text{EOM quotient},\quad
\text{total-derivative quotient},\quad
\text{integrated/cohomological class}.
$$

Do not impose $q=-p$ before forming the raw local jet.

## Second question: settle the HT source normalization fork

The vendored HT source itself contains three apparently incompatible
coefficients:

1. The master operation is printed as

$$
\mathcal D^\triangle_{0,0}(f,g)=\frac12\partial f\,\partial g.
$$

2. The zero-shift component formulas later use coefficient one, e.g.

$$
Q_1((\beta_I)^A b^B)
=\kappa^2\mathbb F^{AB}{}_{DE}
\left(\partial\beta_I^D\partial c^E
+\partial c^D\partial\beta_I^E
+\epsilon_{IJK}\partial\gamma^{JD}\partial\gamma^{KE}\right).
$$

3. The introduction prints a compact $Q_1(C^A(\theta)C^B(\theta'))$ formula
with coefficient $-1/4$, whereas the later compact formula uses coefficient
$-1$.

Trace every prefactor from the master $\mathcal D^\triangle_{0,0}$ definition
to the zero-shift component formula.  List all contributions from:

$$
\frac12\text{ in }\mathcal D^\triangle,quad
\text{ordered-slot symmetrization},\quad
\theta\text{-Taylor factorials},\quad
\epsilon\text{-contractions},\quad
\hbar_{HT},\quad
\kappa_H^2,quad
s_0,s_1.
$$

Use the already matched AA seed to solve the mapping, not to overwrite AB:

$$
\iota Q_{0,HT}\iota^{-1}=s_0\nabla_-^{(0)},qquad
\iota(\hbar_{HT}Q_{1,HT})\iota^{-1}=s_1\Delta_{\rm Project}.
$$

Derive $s_0$, $s_1$, and $\hbar_{HT}\kappa_H^2/\lambda_1$ explicitly.
Then translate the immutable raw $G_3$ vector and the independently obtained
$G_2=1$ through the same map.  State exactly which printed HT equality is the
first inconsistent one if no single mapping satisfies AA, $G_2$, and $G_3$.

No appeal to the desired holomorphic-twist target is permitted.  The answer
must end with one of:

- `G1_NL_PROVED_AND_HT_MAPPING_CLOSED`,
- `G1_NL_RETRACTED_AND_HT_PRINTED_NORMALIZATION_INCONSISTENT`,
- `BLOCKED`, followed by the first missing raw equality.
