# GPT Pro Gate 8: AB/BA G1–G2 external-momentum orientation and common raw normalization

We need locate the first exact error in a standard one-loop triangle calculation. Do not infer a coefficient by matching holomorphic twist. Recompute the momentum typing and raw occurrence multiplicities, then use target-blind residual supersymmetry only as a consistency check.

## Locked DRED mechanism

$$
d=4-2\epsilon,\qquad D_e=r_{e,d}^2,\qquad
\bar r_e^2=D_e+\mu_\ell^2,
$$

$$
N^{(d)}_{e,\rm raw}+K_{e,\rm raw}=0,
$$

for every tagged occurrence in exactly the same normalization. Therefore no free trace factor or fitted $\chi$ is allowed. The finite remainder is the coefficient of

$$
\frac{\mu_\ell^2}{D_0D_1D_2},\qquad
\int\frac{d^d\ell}{(2\pi)^d}\frac{\mu_\ell^2}{D_0D_1D_2}
=\frac1{32\pi^2}.
$$

## Locked routing

Use Fourier phase $e^{+ipx}$ and

$$
r_0=\ell,\qquad r_1=\ell-p,\qquad r_2=\ell-p-q.
$$

Both $p$ and $q$ are declared outgoing from the loop: $p$ at the left action vertex $M/L$, $q$ at the right action vertex $H/R$; source momentum is $-(p+q)$.

The ordered physical pairing is

$$
\langle X,Y\rangle=(\mathfrak p_{\dot\alpha}X)(\mathfrak p^{\dot\alpha}Y),
\qquad \mathfrak p D_{\dot\alpha}=D_{\dot\alpha}.
$$

Every ordinary external derivative obeys $P\mapsto ip$.

## G3 already fixed

The raw PE/PX Hessian replay gives, on edge $e_2$,

$$
P_{B,d}=-512\frac{D_2W_{p0}}{D_0D_1D_2},\qquad
C_{B,d}=+512\frac{W_{p0}}{D_0D_1},
$$

so the full-$d$ sum vanishes and the remainder is $-512\mu_\ell^2W_{p0}/P_D$. The analogous $e_0,e_1$ rows close in the same raw units. The raw-wedge result is $(+i\sqrt2,-i\sqrt2)$ for $(C_2>C_3,C_3>C_2)$ before converting the two momentum operators. Since

$$
(ip)_{\dot\alpha}(iq)^{\dot\alpha}
=-p_{\dot\alpha}q^{\dot\alpha},
$$

the typed result is

$$
(C_2>C_3,C_3>C_2)=(-i\sqrt2,+i\sqrt2).
$$

Do not change this common normalization.

## G2 supplied raw result

The exact original-measure D-algebra gives three metric occurrences

$$
c_1=1-z,\qquad c_{2,\det}=z,\qquad c_{2,\Omega}=-\frac12,
$$

each with its own full-$d$ parent/contact cancellation. Their simplex moments are

$$
2\int_{\Sigma_2}c_1=\frac23,\qquad
2\int_{\Sigma_2}c_{2,\det}=\frac13,\qquad
2\int_{\Sigma_2}c_{2,\Omega}=-\frac12.
$$

Including the current prefactor gives the generic external word

$$
G_2=\frac13 B_1(p)(4p-q)^{\dot\alpha}D_{\dot\alpha}(q).
$$

One audit called the $p$ coefficient $4/3$ the pairing and the $q$ coefficient $-1/3$ a divergence/EOM carrier. An older audit instead combined them as $4/3-1/3=1$. Decide which is correct from the declared outgoing routing and Fourier phases. Explicitly derive:

1. which phase multiplies $B_1$ and which phase multiplies $D$ at the final local product;
2. whether the symbol $q$ in the displayed D-word is the physical momentum of $D$, its negative, or a source-routed momentum;
3. the exact decomposition into $\langle B_1,D\rangle$, $B_1(P\cdot D)$, and a total derivative;
4. which terms vanish in the relevant EOM/total-derivative quotient, with no unsupported $q=\pm p$ substitution.

## G1 supplied raw result

The complete polarized VVV replay has six color permutations and two QL/LQ placements per chirality, action Taylor weight

$$
\frac1{2!}(1+1)=1.
$$

The generic-$p,q$ descendant replay currently reports two marked pieces

$$
G_{1,A}=\frac43p+\frac23q,\qquad
G_{1,B}=\frac23p+\frac43q,
$$

and then adds them to $(2,2)$. But the original one-variable replay was performed with the physical local specialization $q=-p$ and found the two marked pieces $+2/3$ and $-2/3$ before their sum. A different full route table gives yet another vector $(-7/6,-4/3)$, so at least one generic lift is wrong.

Reconstruct G1 directly from the ordered source derivative

$$
\nabla_-(AB_1)=(\nabla_-A)B_1+A(\nabla_-B_1),
$$

keeping the two marked occurrences, their graded sign, outgoing momentum orientation, and all six VVV color permutations. Determine whether the A- and B-marked rows are independent contributions, product-rule presentations of one local derivative, or differ by a contact/EOM/total-derivative term. Produce the exact final coefficients of $\langle D,B_1\rangle$ and $(P\cdot D)B_1$.

## Target-blind covariance gate

The locked bottom-letter laws imply that a covariant result in basis

$$
(\langle D,B_1\rangle,\langle B_1,D\rangle,
\langle C_2,C_3\rangle,\langle C_3,C_2\rangle)
$$

must lie on

$$
\mathbb C(1,1,-i\sqrt2,+i\sqrt2).
$$

Use this only after the raw G1/G2 derivation. Give:

- the first erroneous equality in the existing G1/G2 logic;
- a full factor ledger (Taylor, action ordering, Wick, endpoint, measure, Fourier, color, external map);
- occurrence-wise $N^{(d)}_{e,\rm raw}+K_{e,\rm raw}=0$;
- exact G1 and G2 typed coefficients;
- the final four-component covariance check.

No fitted factor, no appeal to the holomorphic-twist coefficient, and no schematic proportionalities.
