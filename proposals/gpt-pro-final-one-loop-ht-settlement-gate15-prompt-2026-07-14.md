# GPT Pro Gate 15 — final one-loop anomaly-sector / holomorphic-twist settlement

This is the final adversarial review.  Do not restart diagram discovery.  Audit
the following sealed Project result and identify the first false equality if
one exists.

## 1. Universal DRED identity

Let

$$
d=4-2\epsilon,
\qquad
\ell_d^2=\bar\ell^2+\widetilde\ell^2,
\qquad
\mu_\ell^2:=\bar\ell^2-\ell_d^2=-\widetilde\ell^2.
$$

For the internal edge selected by the actual four-dimensional superspace
$D$-word,

$$
\frac{\bar r_e^2}{D_0D_1D_2}-\frac1{\prod_{j\ne e}D_j}
=\frac{\mu_\ell^2}{D_0D_1D_2}.
$$

If the numerator were $r_{e,d}^2$, the parent plus Schwinger cut would vanish
pointwise.  The finite master is

$$
\mu^{2\epsilon}\int\frac{d^d\ell}{(2\pi)^d}
\frac{\mu_\ell^2}{D_0D_1D_2}
=\frac1{32\pi^2}.
$$

Every bare anomaly residue in the completed calculation is an occurrence-wise
$\mu_\ell^2$ cutting failure.  No graph-specific factor $4-d$ is inserted.

## 2. Exact zero-shift physical ledger

Use

$$
\mathbb L=(A,B_1,B_2,B_3,C_1,C_2,C_3,D_{\dot1},D_{\dot2}).
$$

The target-blind Project ledger was completed and sealed before HT was read:

$$
81=81_{\rm COMPLETE\_EXACT}
=29_{\rm nonzero}+52_{\rm zero}.
$$

Its nonzero formulas are

$$
\Delta(A,A)=\lambda_1\mathbb F^{AB}{}_{DE}
\left[
\langle D^D,A^E\rangle-\langle A^D,D^E\rangle
+\sum_{r=1}^3
(\langle B_r^D,C_r^E\rangle-\langle C_r^D,B_r^E\rangle)
\right],
$$

$$
\Delta(A,B_r)=\Delta(B_r,A)
=\lambda_1\mathbb F^{AB}{}_{DE}
\left[
\langle D^D,B_r^E\rangle+\langle B_r^D,D^E\rangle
-i\sqrt2\epsilon_{rst}\langle C_s^D,C_t^E\rangle
\right],
$$

$$
\Delta(A,C_r)=\Delta(C_r,A)
=\lambda_1\mathbb F^{AB}{}_{DE}
\left[D_{\dot a}^D(P^{\dot a}C_r)^E
-(P_{\dot a}C_r)^D D^{E\dot a}\right],
$$

$$
\Delta(B_r,B_s)_{r\ne s}
=\epsilon_{rst}\lambda_1\mathbb F^{AB}{}_{DE}
\left[-i\sqrt2\langle D^D,C_t^E\rangle
+i\sqrt2\langle C_t^D,D^E\rangle\right],
$$

$$
\Delta(B_r,C_s)=\Delta(C_s,B_r)
=\delta_{rs}\lambda_1\mathbb F^{AB}{}_{DE}
\langle D^D,D^E\rangle,
$$

$$
\Delta(A,D_{\dot a})
=\lambda_1\mathbb F^{AB}{}_{DE}
\left[\frac13\langle P_{\dot a}D^D,D^E\rangle
+\frac23\langle D^D,P_{\dot a}D^E\rangle\right],
$$

$$
\Delta(D_{\dot a},A)
=\lambda_1\mathbb F^{AB}{}_{DE}
\left[\frac23\langle P_{\dot a}D^D,D^E\rangle
+\frac13\langle D^D,P_{\dot a}D^E\rangle\right].
$$

The remaining formulas are

$$
\Delta(B_r,B_r)=\Delta(B_r,D_{\dot a})
=\Delta(D_{\dot a},B_r)=0,
$$

$$
\Delta(X,Y)=0,
\qquad
X,Y\in\{C_1,C_2,C_3,D_{\dot1},D_{\dot2}\}.
$$

Flavor, dotted-spinor, Koszul, and color transports are explicit in all 81
rows.  Reverse ordered representatives were not filled by an unproved source
exchange.

## 3. The only non-graph finite settlement

For ordered $AB_1/BA_1$, the six raw carriers give

$$
v_{\rm raw}
=\left(2,2,-\frac13,\frac43,-2i\sqrt2,+2i\sqrt2\right).
$$

With

$$
T_{DB}=X_{DB}+E_{DB},
\qquad
T_{BD}=-X_{BD}+E_{BD},
$$

the common total-derivative quotient is

$$
v_{\rm TD}=(0,1,-2i\sqrt2,+2i\sqrt2).
$$

The exact lower-resolvent vector-frame orbit contributes no missing anomaly
graph.  Residual supersymmetry gives

$$
M_q=
\begin{pmatrix}
1&-1&0&0\\
i\sqrt2&0&1&0\\
-i\sqrt2&0&0&1
\end{pmatrix},
\qquad
\ker M_q=\mathbb C(1,1,-i\sqrt2,+i\sqrt2).
$$

The independently computed AA seed fixes scale one, so the finite
composite-source normal-product term is

$$
\delta v_{\rm fin}=(1,0,+i\sqrt2,-i\sqrt2),
$$

$$
v_{\rm ren}=v_{\rm TD}+\delta v_{\rm fin}
=(1,1,-i\sqrt2,+i\sqrt2),
\qquad
M_qv_{\rm ren}=0.
$$

$\delta v_{\rm fin}$ is not a triangle, Schwinger contact, inverse-square
occurrence, or new cutting failure.  It is the finite normal-product choice;
all bare graph residues still come only from Section 1.

The obsolete full-1PI audit that set the raw $G_3$ scale to one has been
retracted.  The exact full-measure count is

$$
32768\left(\frac14\right)\left(\frac12\right)
=65536\left(\frac14\right)\left(\frac14\right)
=4096.
$$

The first false equality was dropping one of two correlated cycles inside
the outer $1/2$.  The two cycles cancel that outer $1/2$, leaving raw scale
two before the common-TD and finite normal-product layers.

## 4. Holomorphic-twist round trip

The Project 81-row payload was canonicalized and SHA-256 sealed before the HT
artifact was read.  Direct coefficient, sign, output ordering, and extra-jet
comparison gives

$$
N_{\rm direct}=81,
\qquad
N_{\rm match}=81,
\qquad
N_{\rm mismatch}=0.
$$

For every

$$
m,n\in\mathbb Z_{\ge0},
\qquad 0\le k\le m,
\qquad 0\le\ell\le n,
$$

the Project and corrected HT kernels obey

$$
K^P_{m,n;k,\ell}
=\frac{2\binom mk\binom n\ell}
{(m+n+2)(k+\ell+1)},
$$

$$
T^{HT,{\rm printed}}_{m,n;k,\ell}
=\frac{\binom mk\binom n\ell}
{(m+n+2)(k+\ell+1)},
$$

$$
\boxed{
K^P_{m,n;k,\ell}
=2T^{HT,{\rm printed}}_{m,n;k,\ell}
=T^{HT,{\rm corrected}}_{m,n;k,\ell}.}
$$

The finite executable rectangle $0\le m,n\le8$ gives $2025/2025$ exact
kernel coefficients and $141750/141750$ lifted base-output coefficients.
This finite rectangle is a regression, not the proof of the displayed
all-$m,n$ identity.

## 5. Machine settlement

The final physical verifier has

$$
33/33\ {\rm PASS},
\qquad
0\ {\rm FAIL},
$$

and status `ACCEPTED` in scope `PHYSICAL_ONE_LOOP_ANOMALY_SECTOR`.
Broader raw graph $q$-functor, general BV/WZ/open-color evanescent-module,
formal-$U/Q_0$, and general reductive-color theorems remain `OUT_OF_SCOPE`;
they are not used to claim the physical one-loop result.

## Required final verdict

Check Sections 1--5 in order.  If any Project equality fails, return

`FINAL_SETTLEMENT_REJECTED`

followed immediately by the first failed explicit equality and its corrected
right-hand side.  Otherwise end with exactly

`FINAL_ONE_LOOP_HT_SETTLEMENT_ACCEPTED`.
