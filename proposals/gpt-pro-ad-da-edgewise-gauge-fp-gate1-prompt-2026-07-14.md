# GPT Pro Gate 1 — ordered AD/DA edgewise gauge/FP quotient

You are an independent adversarial supergraph reviewer.  The authority base is `origin/main@00000f748fe4bdd1b5d122663cc1fb814faace66`; the local proposal branch is `6bb93d8df5a05af709a364be0d387795a5e18842`.  Do not use a holomorphic-twist coefficient or infer a missing multiplicity from an expected answer.

Close the ordered one-loop anomaly sectors

$$
A>D_{\dot1},
\qquad
D_{\dot1}>A
$$

inside the conditional Fermi--Feynman algebra generated by Step 5A.57--5A.70.

The frozen source identity is

$$
D_-D_+\bar D^2D_+
=-8\det_4(r_e)D_+
-\frac12D_+\bar D^2D^2.
$$

Use

$$
r_0=\ell,
\qquad
r_1=\ell+p,
\qquad
r_2=\ell+p+q.
$$

The routewise selected sums already obtained by exact sparse superspace differentiation are

$$
N_{AD}^{\rm ev}
=\frac{i}{16}\mu_\ell^2
\left(\ell+p+\frac12q\right)_{+\dot1},
$$

$$
N_{DA}^{\rm ev}
=-\frac{i}{16}\mu_\ell^2\ell_{+\dot1}.
$$

For every selected edge impose separately

$$
\frac{r_{e,d}^2}{D_0D_1D_2}
-\frac1{\prod_{j\ne e}D_j}=0,
$$

$$
\frac{\bar r_e^2}{D_0D_1D_2}
-\frac1{\prod_{j\ne e}D_j}
=\frac{\mu_\ell^2}{D_0D_1D_2}.
$$

The gauge-fixing quadratic row is $\Box\mathcal P_0$.  The FP transformation and action must be derived only from Step 5A.57--5A.60.  The separated Nielsen--Kallosh branch is not selected.

After correcting the former dotted-index conflation, the exact contact cores in the canonical DD/F component normalization and the external order left-$\dot1$, right-$\dot2$ are:

$$
\begin{array}{c|cc}
\text{occurrence}&AD&DA\\ \hline
O05, e=0,\ \text{source }\dot1
&7i\,q_+&-\dfrac i2q_+\\[1mm]
O05, e=2,\ \text{source }\dot2
&2i\,q_+&i(p+q)_+\\[1mm]
O06, e=1
&0&i(p+q)_+
\end{array}
$$

The $O06$ DA entry is the exact sum of its chiral and antichiral quartic words.  The sampled $O07$ tadpole is zero.  The matter Euler/current kernel and explicit contact are equal and opposite before regulation; they must still be retained as separate members of the same full-$d$ Schwinger family.  All 42 spectator routes per ordering are connected but 1PR.  The control color component $F_{01;10}$ vanishes for those spectator rows.

Required output:

1. Derive the edgewise identity joining the longitudinal source term to the gauge-fixing, $O05$, and $O06$ contacts.  List each selected inverse-kernel edge and do not cancel fractions with different denominators.
2. Derive the FP Euler bubble with DD external ports directly from Step 5A.57--5A.60.  Print its ordered ghost propagators, cubic vertex, loop sign, $D$-word, full-$d$ cut, and any $\mu_\ell^2$ remainder.  Decide exact zero or exact nonzero.
3. State whether the absent separated NK branch can be omitted within this conditional slice.  Do not assign it a guessed weight.
4. Recompute both orderings independently, including color, Koszul, dotted raising/lowering, loop routing, rank-one simplex moments, and the two distinct external momentum placements.
5. End with a finite occurrence table and the target-blind ordered AD/DA vector.  If the locked data are insufficient, name the first missing operator rather than filling it from a target.

Identify the first invalid assumption in the current local derivation, if any.  Give explicit operator and edge calculations; do not replace them with a verbal gauge-independence argument.
