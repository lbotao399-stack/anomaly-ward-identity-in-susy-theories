# Step-5L memo — the $(\mathfrak b,\mathfrak c)$ coefficient computed to completion: pairing theorem, triangle master, $c_3=-\sqrt2$

Status: `NON_AUTHORITY_PROPOSAL`. Numbered items (5L.$n$). Companion machine
certificate: `scripts/check_step5l_bc_anomaly_assembly.py` (all sections assert-clean).

This memo finishes the computation that 5G carried to "rational backbone × four flagged
global factors" and that 5K §4 left as "steps 2–4 not yet done": it derives the four
factors mechanically, corrects one structural claim of 5K, and produces the first
honest one-loop coefficient of the program.

---

## 1. The pairing theorem (supersedes 5K §4 steps 2–3)

5K §3 planned to sum the six one-vertex triangle numerators T-a…T-f and argued "their
divergences must cancel in the sum". That plan is superseded — the actual structure is
sharper:

> **(5L.1) Pairing theorem.** Under DRED, each one-vertex triangle of the 5K census
> cancels **identically at the integrand level** against the "1"-branch of the
> Schwinger–Dyson collapse of its parent insertion piece — not merely in the sum, and
> not merely after integration:
> - **T-a** ↔ the two "1"-branches of the $\mathcal I_6^{\rm kin}$ boxes
>   ($B_\parallel$, $B_\times$): *machine-certified* (script §7: both cancellations
>   hold identically in symbolic loop momentum, all external index assignments);
> - **T-b** ↔ the $\mathscr D\mathscr D$-contact resolution of
>   $\mathcal E_{\mathscr D}$: exact because the auxiliary SD identity
>   $\langle\mathcal E_{\mathscr D}(x)G\rangle=\hbar\langle\delta G/\delta\mathscr
>   D(x)\rangle$ has no denominators and no $\sigma$-algebra;
> - **T-c/T-d** ↔ the $\mathcal E_A$-SD identity: no graph with an internal $A$-line
>   closes in this channel at one vertex, so the scalar-current and fermion-current
>   triangles cancel *each other* (their sum is the full
>   $\langle\mathcal E_A(\sigma\widetilde\lambda)_-\,G\rangle=0$ identity);
> - **T-e** ↔ the scalar-collapse of $\mathcal E_\phi^{\rm kin}$: exact ($-\ell_d^2$
>   numerator against $\ell_d^2$ denominator, no $\mu^2$, scalar-sector exactness);
> - **T-f** ↔ detection contacts of the $\mathcal E_\lambda$-leg (typed, not anomaly).
>
> Corollary 1: the six triangles never survive as independent contributions and the
> channel has **no pole$\times(-2\epsilon)$ residue** (each pole cancels pairwise at
> the integrand level, before any evanescent contraction can meet it).
> Corollary 2: the entire one-loop correction is the $\mu^2$-branch of the unique
> fermionic collapse site — confirming (5G.7) against the 5K reinterpretation.

## 2. The triangle principle, realized precisely

The owner's principle (5K.P) — triangles carry the coefficient, higher polygons are
covariantization dressing — is realized in the insertion formalism as follows:

> **(5L.2)** The surviving $\mu^2$-branch integral is
> $\int\mu_\ell^2\,\ell_d^2/D^4=\int\mu_\ell^2/D^3-\Delta\!\int\mu_\ell^2/D^4
> =\tfrac1{32\pi^2}+O(\epsilon)$: the box **pinches to the three-denominator
> $\mu^2$-triangle master**. The coefficient is carried by the triangle master exactly
> (the ABJ pattern); genuine four-denominator $\mu^2$-remainders are $O(\epsilon)$
> ((5G.6)$_2$); and the $A_m$-emission graphs generated by the $\mathcal D_m$'s and
> $\mathcal E^{\rm gauge}$-parts of the insertion supply the
> $\partial\to\mathcal D$ covariantization of the output — the dressing, fixed by
> covariance, contributing no independent coefficient.

So "compute the one-loop triangle" = "compute the $\mu^2$-triangle-master branch of
the collapse", which is what is done below.

## 3. Mechanical derivation of the four 5G global factors

`scripts/check_step5l_bc_anomaly_assembly.py` derives, from the locked inputs only
((1.3)–(1.5), (1.51)–(1.52), (5D.D4), (4C.42a) with $e^{-S_E/\hbar}$, left-derivative
Euler convention (5C.13), DRED axiom (5G.5), masters (5G.6)):

| item | derived value | method |
|---|---|---|
| fermion propagator constant | $\langle\psi_a\widetilde\psi_{\dot b}\rangle=-i\hbar g^2(\sigma\!\cdot\!k)_{a\dot b}/k^2$ | explicit Berezin integration (8-mode toy); **confirms (5G.1)** |
| SD collapse constants | $\langle\mathcal E_{\widetilde\psi}\widetilde\psi\rangle=+\hbar\delta$, $\langle\mathcal E_{\psi}\psi\rangle=+\hbar\delta$ ($\chi_R=\chi_L=+1$) | Berezin + left-derivative check against $\partial_LS/\partial\widetilde\psi$ |
| collapse lemma | $ih(\bar\sigma\!\cdot\!\ell)P_{\psi\widetilde\psi}(\ell)=\chi\hbar\big(1+\mu_\ell^2/\ell_d^2\big)\delta$ | symbolic matrix identity + (5G.5) |
| Koszul signs | $B_\parallel:+1$, $B_\times:-1$ | crossing-count on the ordered odd sequence |
| $\epsilon$-chains | $B_\parallel\to+\epsilon^{\dot c\dot d}$, $B_\times\to-\epsilon^{\dot c\dot d}$ | explicit index sums, locked $\epsilon$ values |
| momentum routing | unique orientation solution per box (loop-quadratic, orientation-blind) | integer conservation solve |
| color words | $B_\parallel\to-c_{ACD}c_{BCE}$, $B_\times\to-c_{ACE}c_{BCD}$ ($\kappa$-transport) | explicit su(2) sums |
| flavor | $\delta_{rs}$, uniform in $r$ | explicit sums, asserted |
| tree contact | $=+\sqrt2\hbar F_r\widetilde\phi_s=(-\hbar)\,\delta_-\mathcal O$ | $\mathcal I_5$-collapse, $\epsilon_{-+}=+1$ |

Convention-independence, verified structurally: the fermion-propagator phase enters
once in the collapse and once in the spectator line and its Berezin sign squares away;
the SD constant $\chi$ enters the anomaly and the tree contact once each and cancels
in the ratio. The final coefficient contains **no** convention-sensitive factor.

The certificate (script §7) is the strongest single check: the T-a integrand plus the
"1"-branches of both boxes vanishes identically (symbolic $\ell$, all externals) —
any error in couplings, orderings, $\epsilon$'s, routing, or color would break it.

## 4. Result

$$
\boxed{\;
Q_-\big(\psi^A_{r+}\,\widetilde\phi^B_s\big)
=-\sqrt2\,F^A_r\widetilde\phi^B_s\Big|_{\rm tree}
\;-\;\sqrt2\,\frac{\hbar g^2}{32\pi^2}\,\delta_{rs}\,
{\rm Sym}_C\!\big[c_{ACD}\,c_{BCE}\big]\,
\widetilde\lambda^D_{\dot a}\widetilde\lambda^{E\dot a}
\;+\;O(\epsilon)+O(g^4),\;}
\tag{5L.4}
$$

i.e. in the (5G.13) notation: $\varsigma=-1$,
$\mathbb F^{AB}{}_{DE}={\rm Sym}_C[c_{ACD}c_{BCE}]
=\tfrac12(c_{ACD}c_{BCE}+c_{ACE}c_{BCD})$, and

$$
c_3=-\sqrt2 \qquad\text{in units }\frac{\hbar g^2}{32\pi^2}.
$$

The $D\!\leftrightarrow\!E$-symmetric color word against the antisymmetric spinor
pair emerged from the computation (relative Koszul $\times$ $\epsilon$-chain signs of
$B_\parallel$/$B_\times$) — consistent with the Fermi statistics of
$\widetilde\lambda^D_{\dot a}\widetilde\lambda^{E\dot a}$, a nontrivial internal check.

Status of the 5G residual list: **W6″-a** — certified for the T-a pair (script §7),
typed-exact for T-b…T-f ((5L.1)); **W6″-b** — all four factors derived (table above);
**W6″-c** — confirmed (the $\mu^2$-branch is finite; no pole meets the evanescent
term); **W6″-d** — structural comparison complete (below), absolute normalization
still dictionary-bounded.

## 5. HT (T3) comparison

Target ((5I.1) T3): $Q_1(\beta_I^A\gamma^{JB})=\delta_I^J\kappa^2
f_{ACD}f_{BCE}\,\mathcal D(c^D,c^E)$, $\mathcal D(c,c)=\tfrac12\partial_{\dot
a}c^D\partial^{\dot a}c^E$ (printed kernel; R.5 factor-2 adjudication pending on the
tower side). Match:

- flavor $\delta_I^J\leftrightarrow\delta_{rs}$ ✓; color word identical
  ($f_{ACD}f_{BCE}$, symmetrized) ✓; output $\partial c\partial c\leftrightarrow
  \widetilde\lambda_{\dot a}\widetilde\lambda^{\dot a}$ ✓; $1/32\pi^2$ backbone ✓;
- absolute coefficient: project $-\sqrt2\,\hbar g^2/32\pi^2$ vs HT $\kappa^2$ —
  relatable only through the letter dictionary ($\zeta_Q=\tfrac12$ tree-locked in 5J;
  $\zeta_{\mathfrak d}$ for $\widetilde\lambda\leftrightarrow\partial c$ and the
  $\kappa^2$ conversion are W-J4/W-J5, open). The **sign** is dictionary-phase
  relative (the $\gamma=\widetilde\phi$ Euclidean choice and the $Q\leftrightarrow
  -\tfrac12\boldsymbol\nabla_-$ orientation both flip it); the honest invariant at
  this stage is $|c_3|=\sqrt2$ and the full structure.

The sharp falsifiable target remains the **uniformity test** (5I §3): $c_4$
($\mathfrak b\mathfrak b$-channel) and $c_5$ ($\mathfrak f\mathfrak c$-channel) from
the same $\mu^2$-branch pipeline must, after the one dictionary, collapse with $c_3$
to a single $\kappa^2$. That is the next computation; the pipeline (census → unique
collapse sites → pairing theorem → $\mu^2$-master → engine assembly) now exists
end-to-end and was exercised once completely.

## 6. Honest bounds of this memo

- Gauge group: the engine certifies the **su(2) instance** exactly ($c=\varepsilon$,
  $\kappa=\delta$); the $\kappa$-transport derivation of the word is rank-blind, but
  no other rank has been machine-checked.
- Euclidean only; the Lorentzian mirror is mechanical ((5B.9) workhorse identities)
  and delegated.
- The $\mu^2$-master and the (5G.5) axiom are consumed as recorded ((R.1)–(R.2)
  re-derivations); they are not re-proved here.
- T-b…T-f pairings are typed-exact by the SD arguments of (5L.1); only the T-a pair
  carries a machine certificate. Extending the certificate to T-b…T-f is bounded
  engine work (same builder).
- No claim about derivative-tower members ($m,n>0$) or the gauge channels
  ($\mathfrak f\mathfrak f$, $\mathfrak b\mathfrak f$, $\mathfrak f\mathfrak d$),
  where internal $A$-lines make the $\delta$-vs-$\hat\delta$ (ε-scalar) mechanism and
  $\mathfrak G$-terms live; those need their own census pass.
