# GPT Pro Gate 7: close the AB/BA G3 B-occurrence PE/PX contact exactly

Your Gate 6 trace/metric result is accepted provisionally:

$$
C_D(\chi)=32768\left(\frac14\right)\left(\frac12\right)
\left(-\frac12\right)\chi=-2048\chi,
$$

and the routed A-occurrence full-$d$ Schwinger identity fixes $\chi=1$.

Do not stop at the residual $\Delta_B$.  Close the B-occurrence directly.

Use the locked routing

$$
r_0=\ell,\qquad r_1=\ell-p,\qquad r_2=\ell-p-q,
$$

and the exact original-measure D-word definitions

$$
\mathcal A=D_+\bar D^2D_+\delta_{SM},
$$

$$
\mathcal B=D_+\bar D^2D^2\delta_{SH},
\qquad
\mathcal B_-=D_-\mathcal B,
$$

$$
\mathcal P_{MH}=\bar D_M^2D_M^2\delta_{MH},
$$

$$
N_B=\int d^4\theta_Md^2\bar\theta_H\,
\mathcal A\mathcal B_-\mathcal P_{MH}C_M(p)C_H(q).
$$

The exact sparse replay gives

$$
T_B^{\rm raw}=32768z,
\qquad
2\int_{\Sigma_2}z=\frac13,
$$

and after $\chi=1$

$$
P_{B,E2}=-512\frac{\bar r_2^2W_{p0}}{D_0D_1D_2}.
$$

The antichiral cubic exponent vertex is

$$
V_{H_-}=-\frac{\sqrt2g}{3!\hbar}
\int_-\varepsilon_{rst}c_{ABC}
\bar\varphi_r^A\bar\varphi_s^B\bar\varphi_t^C.
$$

For fixed $(1,2,3)$, differentiate this functional in the exact ordered field variables.  Keep separately:

1. the Euler-potential derivative PE;
2. the explicit-potential derivative PX;
3. the collapsed propagator edge $e_{PE},e_{PX}$;
4. the post-collapse routing and wedge $W_{PE},W_{PX}$;
5. the left/right Berezin and Koszul signs.

Derive, rather than assume, the required identity

$$
-D_{e_{PE}}W_{PE}+D_{e_{PX}}W_{PX}=4D_2W_{p0},
$$

or give the corrected exact identity if that displayed form is wrong.  Then prove

$$
P_{B,E2}\big|_{\mu_\ell^2=0}+C_{B,d}=0,
$$

and calculate the unique finite remainder.  No untagged PE/PX cancellation is allowed.

Finally give the completely closed target-blind $G_{3,2}$ and $G_{3,3}$ coefficients, including reflection, with no $\Delta_B$ and no use of the holomorphic-twist coefficient.
