# GPT Pro Gate 13 — target-blind AB/BA common-layer closure

Gate 12 is locked:

$$
c_{G_3}=4096,
\qquad
32768\left(\frac14\right)\left(\frac12\right)
=65536\left(\frac14\right)\left(\frac14\right)=4096.
$$

Do not reopen the measure, transverse-rank, color, or two-cycle factors.

The Gate-10 proposed extra (G_1) longitudinal numerator is also retracted.
The exact 24-word replay shows that the claimed numerator has the wrong
polynomial degree and is not a new graph.

## Locked target-blind local data

Use the ordered local basis

$$
(D>B_1,\ B_1>D,\ C_2>C_3,\ C_3>C_2).
$$

The currently enumerated parent-plus-full-(d)-Schwinger-cut triangles give

$$
v_{\rm local}=(2,1,-2i\sqrt2,+2i\sqrt2).
$$

The three graph families are:

$$
G_1:(c_{\rm pair},c_{\rm EOM})=(2,2),
$$

$$
G_2:(c_{\rm pair},c_{\rm EOM})=
\left(-\frac13,\frac43\right),
\qquad
[G_2]_{\rm dotted\ divergence}=1,
$$

$$
G_3=(-2i\sqrt2,+2i\sqrt2).
$$

For (G_3), all three inverse-square occurrences are paired edgewise:

$$
P_e+K_e\big|_{\mu_\ell^2=0}=0,
\qquad
R_{G_3}=-\frac{4096\mu_\ell^2(p_+\wedge q_+)}{D_0D_1D_2}.
$$

The current (G_1) audit proves only the parent word.  It does **not** prove
the formerly assumed raw identity (C_L=-L_A) as an (R_2) Hessian/cut.

## Suspected missing legal layer

The replay inserted only the leading stripped chiral-frame source

$$
\mathcal O_{\rm phys}^{(0),AB}
=-\frac{g^2}{4\sqrt2}
(D_+\bar D^2D_+u)^A(D_+\phi_1)^B.
$$

But the accepted letters are vector-frame objects:

$$
A_V=\mathcal B_{\rm ad}A_c\mathcal B_{\rm ad}^{-1},
\qquad
B_{1,V}=\mathcal B_{\rm ad}B_{1,c},
$$

and the full insertion must include all same-order bridge/letter-expansion,
link, collapsed, seagull, and genuine Schwinger-Hessian contact terms.

## Required derivation

1. Starting from the vector-frame definitions and the displayed leading
source, expand the complete (AB_1) insertion to precisely the order needed
for one-loop three-propagator and collapsed two-propagator graphs.  List every
new typed port word and its coefficient.  Do not use a desired answer.

2. Enumerate which new words can close with the locked action vertices into a
one-loop graph.  For every survivor give the ordered internal edges, Taylor /
Hessian factor, fermion sign, color order, and pre-(D) coefficient.  Prove all
other words absent by ports.

3. Perform the (D)-algebra only far enough to expose every four-dimensional
inverse square.  Pair each with its full-(d) Schwinger cut.  Any term without
such a square has zero anomaly sector.

4. Resolve the genuine (G_1) contact: derive the raw (R_2) Hessian/cut from
the action and insertion.  Do not set it equal to (-L_A) by definition.

5. Add the new target-blind anomaly residues to (v_{\rm local}).  State the
resulting vector before any total-derivative, EOM, BRST, or cohomology quotient,
and after each quotient separately.

6. Only after sealing that vector, test the local residual supersymmetry
identity.  The test is not an input:

$$
q_1\Delta(AB_1)+\Delta(q_1AB_1)=0.
$$

7. If the vector-frame bridge is a gauge choice and contributes no independent
graph, identify instead the first omitted legal topology in the locked graph
grammar.  If no topology is omitted, exhibit the unique finite local
counterterm/change of composite-operator scheme and derive its coefficient
from a Project Ward identity, not from the holomorphic-twist target.

End with exactly one verdict:

`MISSING_GRAPH_ORBIT_FOUND`, `FINITE_RENORMALIZATION_REQUIRED`, or
`NO_COMMON_LAYER_CLOSURE`, together with the first false equality in the
current local computation.
